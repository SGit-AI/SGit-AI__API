# Changelog — deploy.sgit.ai v1: The Deploy Section Moves Here

**version** v0.1.4 (repo tag at time of writing)
**date** 2026-08-18
**branch** `claude/repo-access-code-push-eqpzrj` → `dev`
**decision** Dinis: this repo + site become the new site and vault of the deploy
section, hosted at **deploy.sgit.ai** — resolving the 17 Aug thread
(`v0.1.3__thread__where-does-the-deploy-site-live.md`) in favour of a variant of
option A: **one vault** (this repo's `ivpijuvg`), not two.

---

## What changed

1. **The SG/Send team's deploy docs are republished into this vault** — all 17 files of
   vault `fyofmkvr` @ `obj-cas-imm-000f0325258c` (the live source behind sgit.ai/deploy),
   fetched read-only over the live API by our own `pages/reader.js` and written verbatim:
   the deploy hub, 7 targets, 2 runbooks, scripts index, 3 reference pages, `_page.json`.
   `MAINTENANCE.md` updated for the new home (docs move with code; publish = sgit push).
2. **`vault/docs/status.md` — "The refactoring, live"**: the extraction status as a
   dated milestone table, updated by vault pushes in the same commits as the work.
3. **Branding**: vault app and Pages shell restyled to sgit.ai's actual shared-stylesheet
   tokens (light `#faf9f5`, ink `#1c1d21`, teal `#0f766e`, 800-weight wordmark with the
   accent suffix) as `deploy.sgit.ai`, with topbar links to sgit.ai/GitHub/llms.txt and
   an inline SVG favicon.
4. **Sectioned navigation** (`content.json` `sections`, replacing flat `docs`): Start /
   Deploy targets / Runbooks & scripts / Reference / The SGit API / About. Both apps
   resolve the imported guides' **relative markdown links** to in-app navigation.
5. **`pages/llms.txt`** rewritten for the deploy.sgit.ai identity, with provenance and
   the full content map.

## Vault state

New commit `obj-cas-imm-cc513f7c2207`, 26 files; named branch forwarded via the local
in-memory server flow. Same vault id and read key — **no rotation**, constants unchanged.

## Which tests SHOULD / should NOT break

- SHOULD: none — vault id, read key, and store paths are unchanged.
- Should NOT: the full suite — 11 passed locally post-republish; nav-path integrity
  (every `content.json` section item resolves to a vault file) verified in Node over HTTP.

## Deferred / for Dinis

- **DNS + Pages custom domain** (cannot be done from this session): repo Settings →
  Pages → Custom domain: `deploy.sgit.ai` (+ enforce HTTPS once the cert issues), and at
  the sgit.ai DNS provider a `CNAME deploy → sgit-ai.github.io`. The site works at the
  github.io URL meanwhile — all asset/ciphertext paths are relative, so no code change is
  needed when the domain lands.
- **sgit.ai handover**: `deploy/vault.json` on sgit.ai still points at `fyofmkvr`. When
  ready, point it at this vault (`ivpijuvg`, endpoint = a live server holding it, or the
  static origin once cross-origin reads are verified) — and decide the fate of
  `fyofmkvr` (freeze with a pointer, or retire).
- This vault still needs a **push to a production SG/Send server** for the sgit.ai
  deploy page to read it live (no write token in this session).
